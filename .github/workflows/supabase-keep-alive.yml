name: Supabase Keep Alive

on:
  workflow_dispatch:

jobs:
  keep-alive:
    runs-on: ubuntu-latest

    steps:
      - name: Keep Alive Check
        run: echo "Supabase keep-alive workflow is running."